{% block btns %}
    <div class="pull-right">
        <a href="#" class="btn btn-default"><i class="fa fa-search"></i> {{ 'Look'|trans }}</a>
        <a href="{{ path('admin_partner_edit', {'id': id}) }}" class="btn btn-success"><i class="fa fa-pencil"></i> {{ 'Edit'|trans }}</a>
    </div>
{% endblock btns %}