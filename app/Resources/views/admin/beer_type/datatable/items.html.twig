{% block btns %}
    <div class="pull-right">
        <a href="{{ path('admin_beer_type_edit', {'id': id}) }}" class="btn btn-success"><i class="fa fa-pencil"></i> {{ 'Edit'|trans }}</a>
    </div>
{% endblock btns %}